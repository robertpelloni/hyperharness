# Handoff (Archive)

> ⚠️ Historical handoff document retained for legacy context only.

## Canonical handoff

- Use root `handoff.md` for the current implementation queue and verified gap inventory.

## Why this file is archived

This file references an early-era `0.2.3-alpha` "Super AI" context and no longer represents the current Borg architecture/version state.
